# homebrew-tap

yikox 的 Homebrew tap。

## 安装 QuickLauncher

```bash
brew install yikox/tap/quicklauncher
```

或：

```bash
brew tap yikox/tap
brew install quicklauncher
```

从源码现编译，无需下载预编译二进制，也无需 Apple 公证。
